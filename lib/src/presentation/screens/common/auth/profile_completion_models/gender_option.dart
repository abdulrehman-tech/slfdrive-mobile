/// Canonical gender options offered in the profile completion form.
///
/// Kept as raw display strings (matching the original screen) because the form
/// does not yet localize option labels — localization keys will be added
/// alongside any UX work that touches this list.
const List<String> kGenderOptions = ['Male', 'Female', 'Other', 'Prefer not to say'];
