import 'package:flutter/material.dart';

class CarSpec {
  final IconData icon;
  final String label;
  final String value;
  final Color color;

  const CarSpec({
    required this.icon,
    required this.label,
    required this.value,
    required this.color,
  });
}
